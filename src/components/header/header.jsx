import { AppBar, Toolbar } from "@mui/material";
import "./herder.css";

const Header = () => {
    return (
       <AppBar position="fixed" className="header">
          <Toolbar>

          <img src="" alt="" />

          </Toolbar>
        </AppBar>
    );
};

export default Header;